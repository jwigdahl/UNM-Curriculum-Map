<h1>Listing depts</h1>

<table>
  <thead>
    <tr>
      <th>College</th>
      <th>Name</th>
      <th>Acronym</th>
      <th>Org code</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @depts.each do |dept| %>
      <tr>
        <td><%= dept.college_id %></td>
        <td><%= dept.name %></td>
        <td><%= dept.acronym %></td>
        <td><%= dept.org_code %></td>
        <td><%= link_to 'Show', dept %></td>
        <td><%= link_to 'Edit', edit_dept_path(dept) %></td>
        <td><%= link_to 'Destroy', dept, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Dept', new_dept_path %>
